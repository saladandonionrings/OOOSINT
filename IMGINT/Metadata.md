# Text/EXIF chunk(s) found
>If you have this :
```bash
❯ exiftool -all img.png
(...)
Warning                         : [minor] Text/EXIF chunk(s) found after PNG IDAT (may be ignored by some readers) [x2]
```

>You can extract the TEXT chunks with :

```bash


```